#!/usr/bin/env python
"""Evaluation is performed by train.py after best-model selection; retained as an entry-point placeholder."""
raise SystemExit("Use train.py; it evaluates validation during training and test only after model selection.")
