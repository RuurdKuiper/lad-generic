[project]
name = "diffusion-lm"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["torch>=2.5", "transformers>=5.0", "peft>=0.15", "accelerate>=1.12", "datasets>=4.1", "PyYAML>=6", "python-dotenv>=1.0", "tqdm>=4.66", "gradio>=5.0"]

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]
